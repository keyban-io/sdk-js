query KeybanClient_chain($chain: ChainType!) {
  chain(type: $chain) {
    rpcUrl
  }
}

query KeybanClient_addressTokenBalances(
  $chainType: ChainType!
  $address: Address!
) {
  chain(type: $chainType) {
    addressTokenBalances(address: $address) {
      token {
        address
        name
        symbol
        decimals
        iconUrl
      }
      balance
    }
  }
}
