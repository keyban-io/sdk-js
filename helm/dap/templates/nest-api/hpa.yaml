{{- if .Values.nestjs.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dap.fullname" . }}
  labels:
    {{- include "dap.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dap.fullname" . }}
  minReplicas: {{ .Values.nestjs.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.nestjs.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.nestjs.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.nestjs.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.nestjs.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.nestjs.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
