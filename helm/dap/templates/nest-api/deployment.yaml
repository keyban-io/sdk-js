apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dap.fullname" . }}-nest
  labels:
    {{- include "dap.labels" . | nindent 4 }}
spec:
  {{- if not .Values.nestjs.autoscaling.enabled }}
  replicas: {{ .Values.nestjs.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "dap.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.nestjs.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "dap.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.nestjs.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.nestjs.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.nestjs.securityContext | nindent 12 }}
          image: "{{ .Values.nestjs.image.repository }}:{{ .Values.nestjs.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.nestjs.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.nestjs.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 5
            failureThreshold: 50
          resources:
            {{- toYaml .Values.nestjs.resources | nindent 12 }}
      {{- with .Values.nestjs.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nestjs.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nestjs.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
