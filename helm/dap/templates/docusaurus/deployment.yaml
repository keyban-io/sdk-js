apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "dap.fullname" . }}-doc
  labels:
    {{- include "dap.labels" . | nindent 4 }}
spec:
  {{- if not .Values.docusaurus.autoscaling.enabled }}
  replicas: {{ .Values.docusaurus.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "dap.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.docusaurus.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "dap.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.docusaurus.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.docusaurus.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.docusaurus.securityContext | nindent 12 }}
          image: "{{ .Values.docusaurus.image.repository }}:{{ .Values.docusaurus.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.docusaurus.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.docusaurus.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 5
          startupProbe:
            httpGet:
              path: /
              port: http
            periodSeconds: 5
            failureThreshold: 50
          resources:
            {{- toYaml .Values.docusaurus.resources | nindent 12 }}
      {{- with .Values.docusaurus.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.docusaurus.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.docusaurus.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
