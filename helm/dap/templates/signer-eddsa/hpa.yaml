{{- if .Values.signerEddsa.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "dap.fullname" . }}-signer-eddsa
  labels:
    {{- include "dap.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "dap.fullname" . }}
  minReplicas: {{ .Values.signerEddsa.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.signerEddsa.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.signerEddsa.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.signerEddsa.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.signerEddsa.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.signerEddsa.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
